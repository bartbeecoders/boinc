//! Boinc tray application. The Floem UI and tray icon arrive in Phase 3 of
//! `plan.md`; until then this is a scaffolding placeholder.

fn main() {
    println!("boinc-app {}", boinc_core::version());
}
